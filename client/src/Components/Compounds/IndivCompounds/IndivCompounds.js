import React from 'react';
import styled from 'styled-components';
import Layout from '../../Layout/Layout';

/**
 * Parent component for the individual compounds page.
 *
 * @component
 * @example
 *
 * return (
 *   <Compounds/>
 * )
 */
const IndivCompounds = () => (
  <Layout>
    hi
  </Layout>
);

export default IndivCompounds;
