import React from 'react';

/**
 * Shows the stats with graphics on the front page.
 * 
 * @component
 * @example
 * 
 * return (
 *   <Stats/>
 * )
 */
const Stats = () => {
    return (
        <div> stats </div>
    )
};

export default Stats;