/**
 * 
 * @returns {Array} - an array of the dataset names in our database.
 */
const datasets = ['CTRPV2', 'GDSC_v1', 'GDSC_v2', 'GRAY', 'FIMM', 'CCLE', 'gCSI', 'UHNBreast'];

export default datasets;
